# Server-only secrets. Never expose these with a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=
OPENAI_ASSISTANT_MODEL=gpt-5.4-mini

# Optional inquiry notification destination for the Cloudflare deployment.
INQUIRY_NOTIFICATION_WEBHOOK_URL=
